ocharptr lspncprod(ocharptr, ocharptr, int);

ocharptr spncprod(ocharptr, ocharptr, int);

ocharptr tspncprod(ocharptr, ocharptr, int);

ocharptr ltspncprod(ocharptr, ocharptr, int);

ocharptr onmbrnch1(ocharptr, int, int, int);

ocharptr onmbrnch(ocharptr, int, int, int);

termptr compare(termptr, termptr);

ocharptr rcompare(ocharptr, ocharptr);

ocharptr kronk(ocharptr, ocharptr, groop);

prodtype pkronk(prodtype, prodtype);

void onttscalar(ocharptr, ocharptr, groop);

prodtype contract(prodtype, int, int, char *);

ocharptr u1prod(ocharptr, ocharptr);

ocharptr unitprod(ocharptr, ocharptr, bool, int);

ocharptr unddprod(ocharptr, ocharptr, int);

ocharptr sunmbrnch1(ocharptr, int, int, int, int, int);

ocharptr sunmbrnch(ocharptr, int, int, int, int, int);

ocharptr orthprod(ocharptr, ocharptr, bool, int);

ocharptr sympprod(ocharptr, ocharptr, int);

ocharptr su2su6prod(ocharptr, ocharptr);

ocharptr symmprod(ocharptr, ocharptr, int);

ocharptr ebrnch(ocharptr, ocharptr, tabptr);

ocharptr g2brnch(ocharptr);

ocharptr g2prod(ocharptr, ocharptr);

ocharptr e8prod(ocharptr, ocharptr);

ocharptr e7prod(ocharptr, ocharptr);

ocharptr e6prod(ocharptr, ocharptr);

ocharptr f4prod(ocharptr, ocharptr);

ocharptr lsoncprod(ocharptr, ocharptr, int);

ocharptr soncprod(ocharptr, ocharptr, int);

ocharptr unssprod(ocharptr, ocharptr, int);

ocharptr undsprod(ocharptr, ocharptr, int);

ocharptr onttprd(int, ocharptr, ocharptr);

ocharptr onstprd(int, ocharptr, ocharptr);

ocharptr onssprd(int, ocharptr, ocharptr);

ocharptr so2nprd1(ocharptr, ocharptr, int);

ocharptr so2nprd2(ocharptr, ocharptr, int);

ocharptr so2nprd3(ocharptr, ocharptr, int);

ocharptr spnprd(int, ocharptr, ocharptr);

ocharptr ssymmprod(ocharptr, ocharptr, int);

bool generic(ocharptr, ocharptr, groop);

ocharptr covariant(ocharptr, groop);

ocharptr fprod(ocharptr, ocharptr, groop);

ocharptr ffprod(ocharptr, ocharptr, groop);

ocharptr sprch(ocharptr);

bool highlystandard(ocharptr, groop);

bool hstd(ocharptr, groop);
